services:
  jenkins:
    image: dso_jenkins_custom
    container_name: dso_jenkins_custom
    user: root
    ports:
      - "8080:8080"
      - "50000:50000"
    volumes:
      - jenkins_home:/var/jenkins_home
      - /var/run/docker.sock:/var/run/docker.sock

volumes:
  jenkins_home: